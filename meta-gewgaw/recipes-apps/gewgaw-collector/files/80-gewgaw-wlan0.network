# Fallback DHCP for wlan0, used by the submit daemon's opportunistic uplink.
# Low priority (80-) so a dev-pushed 25-wlan0.network from setup-wlan.sh wins.
[Match]
Name=wlan0

[Network]
DHCP=yes

[DHCPv4]
RouteMetric=600
